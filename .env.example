# AI Search Backend - Environment Variables Template
# Copy this file to .env and fill in your actual values

# ==========================================
# REQUIRED ENVIRONMENT VARIABLES
# ==========================================

# MongoDB Database Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# JWT Secret for Authentication (Generate a random 32+ character string)
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random

# Google Gemini AI API Key (Get from Google AI Studio)
GEMINI_API_KEY=your-gemini-api-key-here

# ==========================================
# OPTIONAL ENVIRONMENT VARIABLES
# ==========================================

# Application Environment
NODE_ENV=production

# CORS Configuration (Your Vercel app URL)
CORS_ORIGIN=https://your-app-name.vercel.app

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Server Port (Not needed for Vercel, but useful for local development)
PORT=5000

# ==========================================
# VERCEL DEPLOYMENT NOTES
# ==========================================

# 1. Set these variables in your Vercel project dashboard:
#    - Go to your project settings
#    - Navigate to "Environment Variables"
#    - Add each variable above with your actual values

# 2. MongoDB Atlas Setup:
#    - Create a free cluster at https://cloud.mongodb.com
#    - Create a database user
#    - Add your IP to network access (or 0.0.0.0/0 for all IPs)
#    - Get your connection string

# 3. Google Gemini API:
#    - Go to https://ai.google.dev/
#    - Create a new project or use existing
#    - Enable the Gemini API
#    - Generate an API key

# 4. JWT Secret:
#    - Generate a random string (32+ characters)
#    - You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"